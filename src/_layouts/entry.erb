---
layout: default
---
<% event = resource %>
<%= render Shared::Navbar.new(site: site, resource: resource) %>
<main>
  <%= partial 'post/headline' %>
  <%= partial 'entry/card' %>
</main>
<%= partial 'post/related', locals: { resource: event } %>
